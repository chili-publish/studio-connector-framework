version: 2
updates:
  # github-actions scope only. These PRs run the full build/test;
  # only the PR-title check is skipped for the dependabot[bot] actor
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels:
      - 'No JIRA ticket'
