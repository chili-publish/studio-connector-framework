// TODO: Should be taken from some env configuration file
export const BASE_URL = 'https://api.widencollective.com';
// To avoid CORS issues (especially for 'download') we need to proxy request via debuggern backend
export const PROXY_HOSTS = ['https://previews.us-east-1.widencdn.net'];
