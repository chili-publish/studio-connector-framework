{
  "name": "@chili-publish/connector-cli",
  "version": "1.0.32",
  "main": "index.ts",
  "license": "MIT",
  "bin": {
    "connector-cli": "./bin/connector-cli"
  },
  "dependencies": {
    "@chili-publish/studio-connectors": "^1",
    "chalk": "^4",
    "commander": "^11.1.0",
    "express": "^4.18.2",
    "open": "^9.1.0",
    "prettier": "^3.0.3",
    "quickjs-emscripten": "^0.23.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/handlebars": "^4.1.0",
    "@types/node": "^20.8.6",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1",
    "copyfiles": "2.4.1"
  },
  "scripts": {
    "build": "yarn run tsc && yarn --cwd ./debugger run build && copyfiles -Vf ./debugger/bin/* ./out/debugger/",
    "debugger": "yarn run ts-node src/index.ts debug '../connectors/scaper/connector.ts'",
    "update": "yarn build && npm version patch && yarn pack"
  }
}