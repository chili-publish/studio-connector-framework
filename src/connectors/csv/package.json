{
  "name": "csv",
  "description": "Use a publicly accessible CSV file as a data source. Export from Excel as CSV UTF-8, upload to S3 with Content-Type application/json, and paste the URL.",
  "version": "0.2.0-beta",
  "private": true,
  "author": {
    "name": "CHILI publish",
    "email": "info@chili-publish.com",
    "url": "https://github.com/chili-publish"
  },
  "config": {
    "connectorName": "CSV",
    "type": "data",
    "iconUrl": "",
    "options": {},
    "mappings": {},
    "supportedAuth": []
  },
  "license": "MIT",
  "main": "out/connector.js",
  "dependencies": {
    "@chili-publish/studio-connectors": "^1.39.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "yarn connector-cli build",
    "test": "yarn connector-cli test -t tests.json"
  },
  "devDependencies": {
    "@chili-publish/connector-cli": "^1.9.0"
  }
}
