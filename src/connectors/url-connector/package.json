{
  "name": "url-connector",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "config": {
    "type": "media",
    "connectorName": "URL Connector",
    "iconUrl": "",
    "options": {
      "baseUrl": "",
      "logEnabled": ""
    },
    "mappings": {},
    "supportedAtuh": [
      "staticKey"
    ]
  },
  "license": "MIT",
  "main": "out/connector.js",
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@chili-publish/studio-connectors": "^1.27.0"
  },
   "scripts": {
    "build": "yarn connector-cli build",
    "test": "yarn connector-cli test -t tests.json && yarn connector-cli stress"
  }
}
